type: MeshMetric
mesh: default
name: metrics-default
spec:
  targetRef:
    kind: Mesh
  default:
    sidecar:
      includeUnused: true
      profiles:
        appendProfiles:
          - name: Basic
    applications:
      - path: "/" # application metrics endpoint path
        port: 9103
    backends:
      - type: Prometheus
        prometheus:
          port: 5676
          path: "/metrics"
          tls:
            mode: "Disabled"
