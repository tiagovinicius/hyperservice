name: Build, Auto Versioning and Release Creation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci:
    name: 'CI'
    runs-on: 'ubuntu-latest'
    permissions:
      id-token: write
      contents: read
      repository-projects: write
    steps:
      - uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0
      - uses: 'moonrepo/setup-toolchain@v0'
      - name: Build and version
        run: 'moon ci :version :build'
      - name: Create Git Tag
        run: |
          VERSION="v$(date +%Y%m%d%H%M%S)"  # You can customize this format as needed
          echo "Creating tag: $VERSION"
          git tag $VERSION
          git push origin $VERSION  # Push the tag to the remote
      - name: Generate GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ./hyperservice-device-app/bin/*
            ./hyperservice-control-app/bin/*
            ./hyperservice-cli/bin/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 